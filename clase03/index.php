<?php

    // $archivo = fopen("saludar.txt", "w");

    // fclose($archivo);

    //----------------------------------------------------------------------------

    //$archivo = fopen("saludar.txt", "r");

    //echo fread($archivo, filesize("saludar.txt")); Lee la totalidad, o la cantidad de caracteres que le indique
    
    //fclose($archivo);

    //----------------------------------------------------------------------------

    // $archivo = fopen("saludar.txt", "r");
    // $contador = 0;

    // while(!feof($archivo))
    // {
    //     $palabra = fgets($archivo);

    //     if(!empty($palabra)){

    //         echo $palabra . "<br>";
    //         $contador++;
    //     }

 
    // }

    // echo "Cantidad de archivos leidos: " . $contador;

    var_dump($_POST);


?>